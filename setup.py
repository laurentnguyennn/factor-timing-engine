from setuptools import setup, find_packages

setup(
    name='factor-timing-engine',
    version='2.0.0',
    description='Cross-Sector Factor Timing & Dynamic Allocation Engine',
    packages=find_packages(),
    python_requires='>=3.11',
)
